import React from 'react';
import { render } from '@testing-library/react';

// Minimal test just to verify setup
test('renders without crashing', () => {
  // Just a placeholder test
  expect(true).toBeTruthy();
});